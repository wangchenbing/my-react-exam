- name: 打包项目

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  npm-build:
    - name: Build工作
    runs-on: ubuntu-latest
    steps:
    - name: 读取内容
    - uses: actions/checkout@v4

    - name: 安装依赖
      run: |
        npm install
        npm run build
